function Hello () {
    // A simple React component that returns a greeting message

    const names = "sachin"; // Define a constant variable with a string value
    
    return <h2>Hello {names}</h2>; // Return JSX that includes the variable
}

export default Hello; 